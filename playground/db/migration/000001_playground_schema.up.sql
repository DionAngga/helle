CREATE TABLE tbl_user_accounts (
   id INT primary key,
   username LONGTEXT,
   account LONGTEXT,
   type_account LONGTEXT,
   product_name LONGTEXT,
   account_name VARCHAR,
   currency LONGTEXT,
   card_number LONGTEXT,
   status_account BIGINT,
   finansial_status BIGINT,
   default_account BIGINT,
   sc_code LONGTEXT
); 
